machine:
  services:
    - docker

dependencies:
  override:

test:
  override:
    - ./test/bin/run_tests.sh:
        parallel: true
        timeout: 1200
  post:
    - rsync -av ~/reports/ $CIRCLE_ARTIFACTS/reports/
